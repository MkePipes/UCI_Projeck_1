

 ansible-playbook -i hosts metricbeat-config.yml --key-file NewKey-test1.pem

 ansible-playbook -i hosts metricbeat-playbook.yml --key-file NewKey-test1.pem

 ansible-playbook -i hosts install-elk.yml --key-file NewKey-test1.pem

 ansible-playbook -i hosts metricbeat.cfg --key-file NewKwy-test1.pem

ssh-keygen

apt-get update

sudo apt install docker.io

sudo systemctl status docker

sudo docker pull cyberxsecurity/ubuntu:happy_kirch

sudo docker run -ti happy_kirch/ubuntu bash` to start the container

sudo docker run happy_kirch

sudo docker start happy_kirch

sudo docker attach happy_kirch

docker container list -a

docker ps

ansible-playbook -i hosts <Playbook.yaml> --key-file NewKey-test.pem



